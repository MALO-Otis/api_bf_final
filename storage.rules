rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default: authenticated users can read and write
    // Tighten this later to your exact paths if needed.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
